How to edit bashrc
-sudo gedit ~/.bashrc

# Git Stuff
alias ga='git add'
alias gaa='git add .'
alias gc='git commit -m '
alias gp='git push github'
alias gs='git status'
alias gl='git log'

# Git Branches, Path and Color
case "$TERM" in
xterm*|rxvt*)
  PS1='\[\033[0;37m\]\[\033[0;32m\][\u]\[\033[1;33m\] \w\a\[\033[0;31m\]`git branch 2>/dev/null | grep \* | head -1 | sed "s/\* //g" | awk "{ print \" [\"\\\$1 \"]\" }"`\[\033[0m\] \$ '
    ;;
*)
    ;;
esac

